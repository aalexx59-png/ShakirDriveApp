name: shakir_drive
description: Application Flutter - SHAKIR DRIVE
publish_to: "none" # On ne publie pas directement sur pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true

  assets:
    - assets/icon/shakir_drive.png

metadata:
  author: "aalexx59@gmail.com"
  organization: "SHAKIR"
