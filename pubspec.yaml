name: shakir_drive_app
description: Shakir Drive - Offres cartes prépayées (Flutter)
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # (Tu pourras ajouter d’autres packages plus tard)

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo_shakir_drive.png
    - assets/images/promo_3plus1.jpg
    - assets/images/promo_solo_family.jpg
