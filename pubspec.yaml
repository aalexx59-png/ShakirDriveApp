name: shakir_drive_app
description: Application Flutter - Shakir Drive (cartes prépayées)
publish_to: 'none' # tu ne publies pas sur pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # UI & utilitaires
  cupertino_icons: ^1.0.6
  provider: ^6.0.5
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icon/shakir_drive.png
